const {
    mongo_hostname,
    mongo_port,

    mqtt_hostname,
    mqtt_port,

    influxdb_hostname,
    influxdb_port

} = process.env;


module.exports = {
    mongo_hostname,
    mongo_port,

    mqtt_hostname,
    mqtt_port,

    influxdb_hostname,
    influxdb_port
}